$ grpc
A simple, easy-to-use, and developer-friendly CLI tool for gRPC.

Usage:
  grpc <address> [<method> [<json>...]] [flags]
  grpc [command]

Available Commands:
  completion  Outputs the completion script for the specified shell
  help        Help about any command
  server      Starts a gRPC server serving grpcmd.GrpcmdService with reflection
  version     Outputs the version

Flags:
  -h, --help             help for grpc
  -P, --paths strings    comma-separated list of proto import paths.
  -p, --protos strings   comma-separated list of proto files.
  -v, --version          version for grpc

Use "grpc [command] --help" for more information about a command.

$ grpc help
A simple, easy-to-use, and developer-friendly CLI tool for gRPC.

Usage:
  grpc <address> [<method> [<json>...]] [flags]
  grpc [command]

Available Commands:
  completion  Outputs the completion script for the specified shell
  help        Help about any command
  server      Starts a gRPC server serving grpcmd.GrpcmdService with reflection
  version     Outputs the version

Flags:
  -h, --help             help for grpc
  -P, --paths strings    comma-separated list of proto import paths.
  -p, --protos strings   comma-separated list of proto files.
  -v, --version          version for grpc

Use "grpc [command] --help" for more information about a command.

$ grpc -h
A simple, easy-to-use, and developer-friendly CLI tool for gRPC.

Usage:
  grpc <address> [<method> [<json>...]] [flags]
  grpc [command]

Available Commands:
  completion  Outputs the completion script for the specified shell
  help        Help about any command
  server      Starts a gRPC server serving grpcmd.GrpcmdService with reflection
  version     Outputs the version

Flags:
  -h, --help             help for grpc
  -P, --paths strings    comma-separated list of proto import paths.
  -p, --protos strings   comma-separated list of proto files.
  -v, --version          version for grpc

Use "grpc [command] --help" for more information about a command.

$ grpc --help
A simple, easy-to-use, and developer-friendly CLI tool for gRPC.

Usage:
  grpc <address> [<method> [<json>...]] [flags]
  grpc [command]

Available Commands:
  completion  Outputs the completion script for the specified shell
  help        Help about any command
  server      Starts a gRPC server serving grpcmd.GrpcmdService with reflection
  version     Outputs the version

Flags:
  -h, --help             help for grpc
  -P, --paths strings    comma-separated list of proto import paths.
  -p, --protos strings   comma-separated list of proto files.
  -v, --version          version for grpc

Use "grpc [command] --help" for more information about a command.
